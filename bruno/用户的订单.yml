info:
  name: 用户的订单
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:7788/user/getOrder/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
