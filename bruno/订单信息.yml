info:
  name: 订单信息
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:7788/order/getOrder?id=101
  params:
    - name: id
      value: "101"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
